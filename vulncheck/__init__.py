"""vulncheck — a meta-testable, execution-grounded, catalogue-tier code checker.

Built as the executable form of the Rock Lee doctrine's conclusion about
checkers: a checker is a bar-raiser, never a sealer. It is complete over the
classes it encodes (the past) and silent — honestly silent — on the ones it
doesn't (the future, which stays imagination-bound). Every check is an artefact
that must pass its own falsifiable self-tests before it is trusted, and the
whole tool is graded against ground truth, so the 'who checks the checker'
regress bottoms out on facts, not more judgement.
"""
